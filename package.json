{
  "name": "learn cron and node mailer simple case",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "tsc": "rm -rf build/ && tsc",
    "ts": "rm -rf build/ && tsc -w",
    "dev": "nodemon ./build/app.js"
  },
  "dependencies": {
    "node-cron": "^3.0.0",
    "nodemailer": "^6.7.3",
    "nodemon": "^2.0.15",
    "typescript": "^4.6.3"
  }
}